struct:
	id: person
	body:
		name(string)("")
		age(int)(0)

func:
	id: hello
	args: name(const string&)
	rval: string("")
	head: format(const string)("hello ")
	body:
		rval = format + name;
		callb if_a

if:
	id: hello:if_a
	cond: name.length() && name[0]>='a' && name[0]<='z'
	body:
		name[0] -= 32;
